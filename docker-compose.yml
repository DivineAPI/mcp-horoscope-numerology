version: "3.8"

services:
  horoscope-numerology-mcp:
    build: .
    ports:
      - "8003:8000"
    environment:
      - MCP_TRANSPORT=http
      - DIVINE_API_KEY=${DIVINE_API_KEY:-}
      - DIVINE_AUTH_TOKEN=${DIVINE_AUTH_TOKEN:-}
      - MCP_HOST=${MCP_HOST:-mcp.divineapi.com}
      - MCP_JWT_SECRET=7882c91adfcff7e199ae5559b894cbdc950a2722c9949b282df889543fe04373
    restart: unless-stopped
